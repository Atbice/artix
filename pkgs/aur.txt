# AUR packages — installed via `paru -S --needed` by bootstrap.sh after paru
# itself is bootstrapped from a git clone of paru-bin.
# One per line, '#' comments and blank lines ignored.

# --- shell ------------------------------------------------------------------
# Noctalia v5 (native Wayland/GLES rewrite) is NOT on AUR yet — only the
# legacy v4 (QML/Quickshell) is. bootstrap.sh builds v5 from source instead
# (see docs/03-shell-noctalia.md). When v5 lands on AUR, move it here.

# --- gaming -----------------------------------------------------------------
faugus-launcher         # Faugus, the clean way: AUR package, no source build,
                        # bundles UMU, auto-fetches GE-Proton.

# --- extras (commented; uncomment what you want) ----------------------------
# protonup-qt           # GE-Proton/Wine-GE manager — Lutris/Faugus can also do this
# heroic-games-launcher-bin   # Epic/GOG launcher
# bottles                # Wine prefix manager
